package auth;

/**
 * @author shashi
 *
 */
/*
 * public class SimpleAuthenticator implements Authenticator<BasicCredentials,
 * User> {
 * 
 * public Optional<User> authenticate(BasicCredentials credentials) throws
 * AuthenticationException {
 * 
 * if ("secret".equals(credentials.getPassword())) { return Optional.of(new
 * User(credentials.getUsername())); } else return Optional.absent(); }
 * 
 * }
 */